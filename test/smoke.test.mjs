import { describe, expect, it } from "vitest";

// Placeholder -- will be replaced with real dist/index.js and dist/index.cjs import checks once the package builds.
describe("smoke placeholder", () => {
  it("passes trivially until the real build-output checks land", () => {
    expect(true).toBe(true);
  });
});
