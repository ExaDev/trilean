{
  // A no-op Worker whose only purpose is to host json-operators' source as the module graph the workers test suite (test/workers/**) imports against under the real workerd runtime -- there is no fetch handler to exercise, only the library's own Zod schemas and evaluator running inside a Cloudflare Workers isolate to prove they carry zero Node-only API usage.
  "name": "json-operators-workers-test",
  "main": "src/index.ts",
  "compatibility_date": "2024-09-23",
}
