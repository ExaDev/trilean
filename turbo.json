{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": ["pnpm-lock.yaml"],
  "tasks": {
    "_typecheck": {
      "dependsOn": ["_build"],
      "inputs": ["$TURBO_DEFAULT$", "tsconfig.json", "tsconfig.node.json"],
      "outputs": []
    },
    "_lint": {
      "dependsOn": ["_build"],
      "inputs": ["$TURBO_DEFAULT$", "eslint.config.ts"],
      "outputs": [".eslintcache"]
    },
    "_test": {
      "inputs": ["src/**", "vitest.config.ts", "package.json"],
      "outputs": []
    },
    "_test:coverage": {
      "inputs": ["src/**", "vitest.config.ts", "package.json"],
      "outputs": ["coverage/**"]
    },
    "_test:integration": {
      "inputs": ["src/**", "package.json", "vitest.config.ts", "test/integration/**"],
      "outputs": []
    },
    "_test:smoke": {
      "dependsOn": ["_build"],
      "inputs": ["src/**", "tsdown.config.ts", "package.json", "vitest.config.ts", "test/smoke.test.ts"],
      "outputs": []
    },
    "_test:workers": {
      "inputs": ["src/**", "package.json", "vitest.config.ts", "wrangler.jsonc", "test/workers/**"],
      "outputs": []
    },
    "_build": {
      "inputs": ["src/**", "tsdown.config.ts", "package.json", "scripts/**"],
      "outputs": ["dist/**", "schemas/**"]
    }
  }
}
